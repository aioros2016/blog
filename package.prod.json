{
  "name": "blog",
  "version": "0.1.0",
  "scripts": {
    "start": "cross-env NODE_ENV=development craco start",
    "build": "cross-env NODE_ENV=production craco build",
    "test": "craco test"
  }
}
